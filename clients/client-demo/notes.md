# Client deployment notes — client-demo

Demo client folder for multi-client deployment architecture validation.
**Not a live deployment.** Placeholder values only.

## Values

- Slug: `client-demo`
- Domain: `demo.example.com` (placeholder)
- Sabre: **disabled** (`modules.json` + `env.production.example`)
- Dev CP: **disabled**

Copy `env.production.example` to the server `.env` when deploying a real demo instance.
See `docs/client-deployment-architecture.md` and `docs/new-client-deployment-checklist.md`.
