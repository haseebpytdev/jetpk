# JSON contract

Shortened system prompt (~269 est. tokens) requests text fields + patch keys; no policy dump.

Post-audit (non-thinking):

```
JSON_GRAMMAR_TESTED=NO (llama grammar not required; JSON rate already 100%)
VALID_JSON_RATE=100%
SCHEMA_VALID_RATE=100%
SYSTEM_PROMPT_TOKENS≈269
```

Semantic correctness remains separate from JSON validity.
