# PDF export

On-demand DomPDF from structured fields (`resources/views/pdf/visa-document.blade.php`).  
PDF_EXPORT_ON_DEMAND=YES  
PDF_RENDERER_SSRF=PASS (no remote fetch during render; Blade uses local fields only)
