# Fixture transport

`FixtureVisaHttpTransport` + `tests/Fixtures/Visa/mofa/*`

Emulates: GET lookup, captcha image, POST 302 → PrintedUmrahVisa, structured HTML.

No external MOFA calls in automated tests.
