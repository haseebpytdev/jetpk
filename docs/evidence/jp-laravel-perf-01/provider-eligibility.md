# Provider eligibility

Eligibility evaluated once into `skipByConnectionId` before fan-out.

`PROVIDER_ELIGIBILITY_P95_MS` ≈ 10 ms.

Semantics unchanged (module gates, active flags, Sabre channel gates, public routing guard).
