# Assets Required From Owner (Optional)

These do **not** block engineering deployment. CMS authority accepts uploads when provided.

1. **Mobile hero background** (`hero_background_mobile`) — optional tablet/mobile-specific hero.
2. **Support CTA illustration** (`support_cta_background`) — optional; gradient fallback renders today.
3. **Featured deal card photography** — optional per slot; inventory/commercial data resolves without custom images.

No action required for DXB/JED/LHR/IST destination cards if current published CMS assets remain acceptable.
