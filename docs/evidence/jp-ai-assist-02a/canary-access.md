# Canary access

CANARY_ACCESS_AUTHORITY=`staff.support.view` OR Platform Admin (server-side `AiAssistantEligibility`)  

Non-canary anonymous/customer: API 503 unavailable; `ai_assistant_enabled=false` in config.
