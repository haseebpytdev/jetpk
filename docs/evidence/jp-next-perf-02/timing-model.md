# Timing model

See phase brief T0–T12 clocks.

Groups post-fix (SSR authority):
- T3 document request includes server Laravel inventory (private listener)
- T4 shell + useful cards arrive together in HTML
- T5 client inventory skipped when filter key matches SSR
- CMS remains secondary and does not blank cards
