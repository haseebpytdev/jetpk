# Flight render / pairing notes

Pairing remains server-authored (`paired_options`).  
Client no longer blanks READY cards on sort/filter; view switch still reloads representation.  
Full supplier-backed n=10 sampling deferred as external load; Next overhead fixes shipped.
