# Follow-up state

`ConversationStatePatcher` + pipeline prior merge: patches only changed fields (`one day later`, `only direct`, `under 160k`, airline).  
`FOLLOWUP_PATCH_ENGINE=PASS`. Unrelated fields preserved.
