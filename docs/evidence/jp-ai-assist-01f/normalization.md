# Normalization

`LanguageNormalizer` applies bounded Roman Urdu / misspelling replacements (`sy→se`, `chahye→chahiye`, `baghair stop→direct`, etc.) and detects `en` / `ru` / `ur` / `mixed`.

Urdu script matching uses Unicode-aware patterns in resolvers. Not a general translator.
