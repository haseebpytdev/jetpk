# Date resolver

`DateExpressionResolver` handles today/tomorrow/kal/parso/next Friday/aglay jumay/absolute dates/one day later/N din baad.  
`18 ko` without month → clarify (`SILENT_WRONG_DATE_RESOLUTION=0`). Session timezone via Carbon.
