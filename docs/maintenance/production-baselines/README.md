# Production baselines (normalized)

Recovered/production baseline snapshots previously stored at repository root as `_production_baselines/`.

These are historical reference copies for recovery/comparison — not runtime application code.
